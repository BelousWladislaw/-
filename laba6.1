#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
	
void main() {
	setlocale(LC_ALL, "Russian");
	int n, i, sum, num, *pa;
	
	printf("Ââåäèòå ðàçìåð ìàññèâà: ");
	scanf("%d", &n);
	
	pa=(int*)malloc(n*sizeof(int));
	if(pa==NULL) {
		printf("Íåäîñòàòî÷íî ïàìÿòè!");
		getch();
		return;
	}
	printf("Ââåäèòå ýëåìåíòû ìàññèâà: ");
	for(i=0; i<n; i++)
	scanf("%d", pa+i);
	
	printf("Èñõîäíûé ìàññèâ: ");
	for(i=0; i<n; i++)
	printf("%d", *(pa+i));
	num = 0;
	for(i=0; i<n; i++) {
		if(*(pa+i)>0)
		num++;
	}
	printf("\nÊîëè÷åñòâî ïîëîæèòåëüíûõ ýëåíòîâ â ìàññèâå: %d", num);
	sum = 0;
	i = n-1;
	while(*(pa+i)!=0 && i>0) {
		sum = sum+*(pa+i);
		i--;
	}
	printf("\nÑóììà ýëåìåíòîâ ìàññèâà, ðàñïîëîæåííûõ ïîñëå ïîñëåäíåãî íóëÿ: %d", sum);
	free(pa);
	getch();
}
