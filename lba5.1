#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
	void main() {
	setlocale(LC_ALL, "Russian");
	int i, j, n, m;
	int matrix[n][m];
	printf("Ââåäèòå êîëè÷åñòâî ñòðîê: ");
	scanf("%d", &n);
	printf("Ââåäèòå êîëè÷åñòâî ñòîëáöîâ: ");
	scanf("%d", &m);
	for (i=0; i<n; i++) {
		for (j=0; j<m; j++) {
			printf("Ââåäèòå ÷èñëî matrix[%d][%d] ", i, j);
			scanf("%d", &matrix[i][j]);
		}
	}
	printf("\n");
	
	int number, k;
	int num[5];
	for (i=0, k=0; i<n, k<n; i++, k++) {
		number = 0;
		for (j=0; j<m; j++) {
			if (matrix[i][j] == 0) {
				number++;
			}
		}
		num[k] = number;
	}
	for (k=0; k<n; k++) {
		printf("\n");
		printf("%d", num[k]);
	}	
	for (k=0; k<n; k++) {
		if (num[k] = 0) {
			for (i=0; i<n; i++) {
				for (j=0; j<m; j++) {
					if (matrix[i][j] < 0) {
						matrix[i][j] = 0;
					}				
				}
			}
		}	
	}
	for (i=0; i<n; i++) {
		printf("\n");
		for (j=0; j<m; j++) {
			printf("%d ", matrix[i][j]);
		}
	}
}
